const showCompleted = () => ({ type: "SHOW_ONLY_COMPLETED" })

const showAll = () => ({ type: "SHOW_ALL" })

const showActive = () => ({ type: "SHOW_ONLY_ACTIVE" })

export { showCompleted, showAll, showActive }
